// Nessuna API esposta al renderer per ora — il frontend parla con il backend
// solo via HTTP (http://127.0.0.1:8000). Riservato per future IPC sicure
// (es. dialoghi nativi per l'upload di PDF/Note Apple).
